---
phase: 01-parser-model
plan: 4
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - packages/parser/src/resolve-units.ts
  - packages/parser/src/core.ts
  - packages/parser/src/staad/index.ts
  - packages/parser/src/staad/units.ts
  - packages/parser/src/staad/header.ts
  - packages/parser/src/index.ts
  - packages/parser/test/resolve-units.test.ts
  - packages/parser/test/core.test.ts
  - packages/parser/test/index.test.ts
autonomous: true
requirements: [PARSE-02, PARSE-03, PARSE-05]
must_haves:
  truths:
    - "Unit state is stateful: default FEET/KIPS, and mid-file UNIT switches change the conversion applied to all later numeric tokens"
    - "UNIT and STAAD header commands are wired into COMMAND_TABLE — UNIT mutates unit state via applyUnitCommand, STAAD sets ctx.structure so 2D PLANE/FRAME coordinate rows are accepted"
    - "parseStaad(text) is a pure function returning ParseResult { model, warnings } — it never throws on unknown commands"
    - "Blocks are segmented from tokenized entries; unknown command blocks yield UNKNOWN_COMMAND warnings and their body is skipped"
    - "finalize() computes model bounds and links members to sections"
  artifacts:
    - path: packages/parser/src/resolve-units.ts
      provides: "Unit-state machine (length/force factors, mid-file switches)"
      exports: ["createUnitState", "applyUnitCommand", "toMeters"]
    - path: packages/parser/src/core.ts
      provides: "Block segmentation, command canonicalization, ParseContext, finalize"
      exports: ["segmentBlocks", "canonicalizeCommand", "createContext", "finalize"]
    - path: packages/parser/src/staad/index.ts
      provides: "Command dispatch table + registration API"
      exports: ["COMMAND_TABLE", "registerCommand"]
    - path: packages/parser/src/index.ts
      provides: "Public parseStaad entry point"
      exports: ["parseStaad"]
  key_links:
    - from: "packages/parser/src/index.ts"
      to: "packages/parser/src/tokenizer.ts"
      via: "parseStaad calls tokenize(text) first"
      pattern: "tokenize"
    - from: "packages/parser/src/index.ts"
      to: "packages/parser/src/staad/index.ts"
      via: "dispatch loop looks up COMMAND_TABLE; unknown keys push UNKNOWN_COMMAND"
      pattern: "COMMAND_TABLE"
    - from: "packages/parser/src/core.ts"
      to: "packages/parser/src/resolve-units.ts"
      via: "UNIT command handler mutates ctx.units via applyUnitCommand"
      pattern: "applyUnitCommand"
    - from: "packages/parser/src/staad/units.ts"
      to: "packages/parser/src/resolve-units.ts"
      via: "unitsHandler dispatches UNIT blocks into applyUnitCommand (wires the Task 1 machine into dispatch)"
      pattern: "applyUnitCommand"
    - from: "packages/parser/src/staad/header.ts"
      to: "packages/parser/src/core.ts"
      via: "staadHeaderHandler sets ctx.structure consumed by the JOINT COORDINATES handler (01-05)"
      pattern: "structure"
---

<objective>
Build the tolerant parse pipeline: stateful unit machine, block segmentation with command canonicalization, and the public `parseStaad` entry with warning collection.

Purpose: PARSE-02 (unit state machine — PITFALLS P1), PARSE-03 (tolerance — unknown commands warn, never fatal), and the PARSE-05 spine (ParseResult assembly + finalize). Everything later registers into COMMAND_TABLE; with an empty table this plan already parses any file without crashing.

Output: resolve-units.ts, core.ts, staad/index.ts (dispatch table), staad/units.ts + staad/header.ts (core-state handlers), index.ts (public API) + tests.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@packages/parser/src/types.ts
@packages/parser/src/tokenizer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit-state machine (TDD)</name>
  <files>packages/parser/src/resolve-units.ts, packages/parser/test/resolve-units.test.ts</files>
  <behavior>
    - Test 1: fresh state has length FEET, force KIPS (P1 default)
    - Test 2: applyUnitCommand('UNIT METER KN') switches state to M/KN
    - Test 3: applyUnitCommand('UNIT INCH') then toMeters(12, state) === 0.3048; 'UNIT FEET' then toMeters(1, state) === 0.3048
    - Test 4: alias variants accepted: 'UNIT MMS', 'UNIT FE' (FEET), 'UNIT ME' (METER), 'UNIT KIP' (KIPS) per P1 abbreviation rules
    - Test 5: switching units emits UNIT_CHANGE warning; a redundant 'UNIT METER KN' when already M/KN emits no warning
    - Test 6: malformed unit line (e.g. 'UNIT BANANA PINEAPPLE') does not change state and emits MALFORMED_LINE
  </behavior>
  <action>Write resolve-units.test.ts first (RED), then implement resolve-units.ts (GREEN). Stateful machine per PITFALLS P1 + TR.3 factor table: IN=0.0254, FT/FE=0.3048, ME=1, DM=0.1, CM=0.01, MM=0.001, KM=1000 meters; force tokens KIP/KIPS/KN/N/KG/LB/MT recorded for display only (force does not affect geometry conversion). createUnitState() returns { length:'FEET', force:'KIPS' }; applyUnitCommand(line, state, warn) parses a tokenized 'UNIT ...' entry, normalizes aliases, mutates state, returns warnings via a callback (D-06/D-07: UNIT_CHANGE with the line number; MALFORMED_LINE for unrecognized tokens). toMeters(value, state). Use Map-based unit factor tables (never plain objects keyed by unit names per PITFALLS security). Do not parse numbers here — accept already-typed numeric input.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/resolve-units.test.ts</automated>
  </verify>
  <done>All unit-state tests green: defaults, mid-file switch, aliases, warning emission rules.</done>
</task>

<task type="auto">
  <name>Task 2: Block segmentation, canonicalization, context, finalize</name>
  <files>packages/parser/src/core.ts, packages/parser/test/core.test.ts</files>
  <action>Implement core.ts (no DOM/Three imports): (1) canonicalizeCommand(tokens) — uppercase first tokens, collapse spaces, map aliases (INCIDENCE→INCIDENCES, PROPERTY→PROPERTIES, JNT COORD→JOINT COORDINATES, LOADING→LOAD, MEMB→MEMBER per P2 abbreviations) returning the canonical command key; (2) segmentBlocks(entries: TokenizedLine[]) → CommandBlock[] where a block = { name tokens (header entry), bodyLines (subsequent entries), line }. Segmentation rule (tolerant, per ARCHITECTURE Pattern 3): an entry whose first token starts with an uppercase letter is a NEW block header; entries starting with digit, `_`, quote, or `-` are body of the current block. Scoped exceptions: after a DEFINE ... header, absorb body until the matching END DEFINE ... header; after START ... header, absorb until END ... (handles DEFINE MATERIAL START/END, START GROUP DEFINITION/END GROUP DEFINITION, DEFINE REFERENCE LOADS, START JOB INFORMATION/END JOB INFORMATION, START CONCRETE DESIGN/END CONCRETE DESIGN). Tolerant termination for un-terminated DEFINE blocks (checker #8): any DEFINE ... block WITHOUT a matching END DEFINE absorbs until the next top-level LOAD / PERFORM ANALYSIS / PAGE / PRINT / DEFINE header — this covers DEFINE IBC 2015 (real file line 908, no END DEFINE; terminated by DEFINE WIND LOAD at line 914) and DEFINE WIND LOAD (line 914; terminated by the next LOAD header). (3) createContext() → ParseContext { units, warnings[], nodes: Node[], members: Member[], sections: Map, supports: Support[], loadCases: LoadCase[], groups: Map<string, Group>, namedSections: Map<string, SectionProfile & SectionMeta>, memberSectionLinks: Map<number,string>, structure: 'SPACE'|'PLANE'|'FRAME', bounds } with Map-based entity lookups per PITFALLS security. (4) finalize(ctx) → StaadModel: compute bounds min/max from node coords (P8 note: bounds only; centroid rebase is Phase 2 REND-06), assign 0-based internal indexes via array position while preserving 1-based source ids as fields (D-04), link members→sections: direct links from memberSectionLinks, then for members in a group whose name matches a namedSections key, set sectionKey and copy the profile into sections Map. core.test.ts: segmentation of a mixed block body (uppercase/digit/_ entries), DEFINE scoped absorption, canonicalize alias cases, finalize bounds + member-section linking on a tiny fixture built from handwritten entries.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/core.test.ts</automated>
  </verify>
  <done>Segmentation, canonicalization, finalize tests green; finalize preserves 1-based ids and computes bounds.</done>
</task>

<task type="auto">
  <name>Task 3: Public parseStaad entry + dispatch</name>
  <files>packages/parser/src/staad/index.ts, packages/parser/src/index.ts</files>
  <action>Implement staad/index.ts: export COMMAND_TABLE: Record<string, CommandHandler> and registerCommand(canonicalNames: string[], handler) which inserts the handler under each canonical key (including alias keys). Export type CommandHandler = (ctx: ParseContext, block: CommandBlock) => void. Dispatch resolves a block's canonical key as the LONGEST registered key that prefixes the header tokens (multi-token keys like 'MEMBER PROPERTY' / 'JOINT COORDINATES' beat single-token 'MEMBER' / 'JOINT'; single-token keys like 'UNIT', 'STAAD', 'MATERIAL', 'BETA', 'SELFWEIGHT' match their own headers). Core-state handlers register in THIS plan; geometry/load handlers register in 01-05/06/07/08 via module import side effects; 01-09 wires the production import.

Create staad/units.ts: unitsHandler registered under 'UNIT' — it passes the block header entry tokens (e.g. `UNIT METER KN` at real line 14) to applyUnitCommand from resolve-units (Task 1), closing the wiring gap (checker #2: the unit-state machine must be reachable from COMMAND_TABLE, else every real file parses coordinates at the FEET default — a 3.28× scale error invisible to count-only tests).

Create staad/header.ts: staadHeaderHandler registered under 'STAAD' — it parses the first block header entry `STAAD PLANE|SPACE|FRAME` (real line 1 is `STAAD SPACE`; plane-2d fixture is `STAAD PLANE`) and sets ctx.structure accordingly (default 'SPACE'). This is the producer for ctx.structure (checker #3): JOINT COORDINATES only accepts 2-coord rows when structure is PLANE/FRAME, so without it D-02's 2D coverage silently drops every plane joint.

Implement index.ts: export parseStaad(text: string): ParseResult — the ONLY public entry per ARCHITECTURE Pattern 1. Flow: guard input size (text.length > 64_000_000 → return ParseResult with model { empty counts, default units } and a single severity-error warning with a stable code — do NOT throw); tokenize(text); createContext(); segmentBlocks(entries); for each block, canonicalizeCommand(header tokens), longest-prefix look up COMMAND_TABLE — if found, call handler(ctx, block); if not, push UNKNOWN_COMMAND warning with the header line + canonical name (D-07) and skip the block's body (tolerance); finally finalize(ctx) → model; return { model, warnings }. Also export a `registerParsingCommands()` bootstrap in index.ts that imports the two core-state modules created here (staad/units, staad/header) for side-effect registration; 01-09 completes the import list with the remaining handler modules.

test/index.test.ts (parseStaad-level smoke + core-state dispatch tests): (1) parseStaad(loadFixture('real/HPP_Main_Building_2.std')) with only the core-state handlers registered must NOT throw, must return warnings containing UNKNOWN_COMMAND (geometry handlers not yet registered), must produce model.units.length === 'M' and force === 'KN' (UNIT METER KN at line 14 applied — the FEET default would leave 'FEET'), and bounds present; (2) parsing an empty string returns a model with 0 nodes and no throw; (3) a mini deck `STAAD PLANE` + JOINT COORDINATES with 2-coord rows parses with node z=0 and NO MALFORMED_LINE (structure PLANE honored — checker #3 coverage); (4) a `STAAD SPACE` deck with a 2-coord row yields MALFORMED_LINE (missing z).</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/index.test.ts test/core.test.ts</automated>
  </verify>
  <done>parseStaad tolerantly parses unknown-command files with warnings, never throws; empty input yields empty model; size guard returns warning result; UNIT wired (model.units reflects UNIT METER KN, not FEET); STAAD header sets ctx.structure (PLANE 2-coord rows accepted, SPACE rows warned).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.std` text → parseStaad | The public entry; all downstream handlers inherit this boundary. Input is attacker-controlled. |
| block body → handlers | Segmentation decides what each handler sees; mis-segmentation must not crash (tolerant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | parseStaad input size | mitigate | 64 MB size guard returns warning result instead of allocating (PITFALLS security: memory-exhaustion DoS) |
| T-04-02 | Tampering | prototype pollution | mitigate | Map-based entity storage everywhere; never plain objects keyed by member/joint numbers (PITFALLS security) |
| T-04-03 | DoS | block mis-segmentation | mitigate | Uppercase-start rule + scoped DEFINE/START absorption; unknown blocks skipped, never recursed |
| T-04-04 | Tampering | unit-state drift | mitigate | applyUnitCommand only mutates via validated token aliases; malformed lines leave state unchanged (P1) |
| T-04-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- `npx vitest run test/resolve-units.test.ts test/core.test.ts test/index.test.ts` green
- Real HPP fixture parses without throw through the empty table (tolerant core proven)
- All context maps are `Map` (grep for `Record<number` — must be absent in parser sources)
</verification>

<success_criteria>
- Unit state machine per PARSE-02 with defaults, aliases, mid-file switches, warnings
- Tolerant core per PARSE-03: unknown commands warn, body skipped, never fatal
- parseStaad pure + headless + worker-ready (no DOM/global access); ParseResult assembled per PARSE-05
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-04-SUMMARY.md` when done
</output>