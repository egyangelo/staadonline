---
phase: 01-parser-model
plan: 3
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/parser/test/fixtures/real/HPP_Main_Building_2.std
  - packages/parser/test/fixtures/handwritten/plane-2d.std
  - packages/parser/test/fixtures/handwritten/feet-imperial.std
  - packages/parser/test/fixtures/handwritten/legacy-table.std
  - packages/parser/test/fixtures/handwritten/continuations.std
  - packages/parser/test/fixtures/handwritten/unknown-commands.std
  - packages/parser/test/fixtures/loadFixture.ts
  - packages/parser/test/fixtures/manifest.ts
  - packages/parser/test/fixtures/fixtures.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-05]
must_haves:
  truths:
    - "A hermetic golden corpus exists inside the parser package: the real STAAD.Pro file plus hand-written edge-case fixtures"
    - "Fixture loading is deterministic (fixture paths resolve inside packages/parser, never to repo-root staadSample)"
    - "Expected counts for every fixture are recorded in a manifest, derived by deterministic counting helpers"
  artifacts:
    - path: packages/parser/test/fixtures/real/HPP_Main_Building_2.std
      provides: "Copy of the real 92 KB STAAD.Pro file (D-01 primary ground truth)"
      min_lines: 1000
    - path: packages/parser/test/fixtures/handwritten/plane-2d.std
      provides: "2D STAAD PLANE fixture with Z omitted (D-02 edge case)"
    - path: packages/parser/test/fixtures/handwritten/feet-imperial.std
      provides: "FEET/imperial units fixture (D-02 edge case)"
    - path: packages/parser/test/fixtures/handwritten/legacy-table.std
      provides: "Older/unquoted TABLE syntax fixture (D-02 edge case)"
    - path: packages/parser/test/fixtures/handwritten/continuations.std
      provides: "Continuation + semicolon + comment fixture (D-02 edge case)"
    - path: packages/parser/test/fixtures/handwritten/unknown-commands.std
      provides: "Unknown-command tolerance fixture (D-02 edge case)"
    - path: packages/parser/test/fixtures/manifest.ts
      provides: "Expected counts for all fixtures (D-09 dual-tier)"
      contains: "expected"
    - path: packages/parser/test/fixtures/loadFixture.ts
      provides: "loadFixture(name) deterministic text loader"
      exports: ["loadFixture", "FIXTURE_NAMES"]
  key_links:
    - from: "packages/parser/test/fixtures/loadFixture.ts"
      to: "packages/parser/test/fixtures/real/HPP_Main_Building_2.std"
      via: "path.join(FIXTURE_DIR, ...) — hermetic path, no repo-root dependency"
      pattern: "FIXTURE_DIR"
---

<objective>
Assemble the golden corpus and fixture harness that validates the entire parser.

Purpose: D-01 mandates real STAAD.Pro files as ground truth; D-02 supplements with hand-written edge cases; D-09 requires dual-tier validation (exact for hand-written, tolerant+counts for real files). This plan makes the corpus hermetic (tests never depend on the repo-root `staadSample/` path) and records expected counts deterministically.

Output: real + hand-written fixtures, `loadFixture` loader, `manifest.ts` expected counts, and a smoke test proving the harness works.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@staadSample/HPP_Main_Building_2.std
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy real fixture + build deterministic count helpers</name>
  <files>packages/parser/test/fixtures/real/HPP_Main_Building_2.std, packages/parser/test/fixtures/manifest.ts</files>
  <action>Copy `staadSample/HPP_Main_Building_2.std` verbatim into `packages/parser/test/fixtures/real/` (binary-identical copy, byte count must match the 92518-byte source) — this makes the corpus hermetic per the orchestrator's fixture directive. Then implement manifest.ts exporting `expected` = object of per-fixture expected counts. For the REAL fixture, do NOT hardcode magic numbers by hand: implement small deterministic counting helpers in manifest.ts (documented inline) that scan the fixture text with simple string operations — count joint entries as max numeric joint id in the JOINT COORDINATES block, count member rows by counting semicolon-terminated entries in MEMBER INCIDENCES, count element rows in ELEMENT INCIDENCES, count named group entries in START GROUP DEFINITION, count primary load-case headers (lines matching `LOAD <numeric id> LOADTYPE`, 14 in the real file) AND LOAD COMB lines (lines matching `LOAD COMB <id>`, 274 in the real file) — expose loadPrimary, loadComb, and loadCases (= loadPrimary + loadComb, 288 total — checker #9 disambiguation), and detect the first UNIT line tokens. Export `expectedReal` computed by calling these helpers on the loaded real text, and `expectedHandwritten` as exact hand-written constants (each hand-written fixture is small enough to count manually). The manifest is the D-09 expected-count source of truth.</action>
  <verify>
    <automated>node -e "const f=require('fs');const a=f.readFileSync('packages/parser/test/fixtures/real/HPP_Main_Building_2.std').length;const b=f.readFileSync('staadSample/HPP_Main_Building_2.std').length;console.log(a,b);if(a!==b)process.exit(1)"</automated>
  </verify>
  <done>Real fixture copied byte-identical; manifest.ts exports expectedReal (computed) and expectedHandwritten (constants) with positive integer counts.</done>
</task>

<task type="auto">
  <name>Task 2: Write hand-written edge-case fixtures</name>
  <files>packages/parser/test/fixtures/handwritten/plane-2d.std, packages/parser/test/fixtures/handwritten/feet-imperial.std, packages/parser/test/fixtures/handwritten/legacy-table.std, packages/parser/test/fixtures/handwritten/continuations.std, packages/parser/test/fixtures/handwritten/unknown-commands.std</files>
  <action>Per D-02, write five small (< 40 lines each) hand-written fixtures covering edge cases the real file lacks. Each file is a valid .std deck with a trailing `* end of fixture` comment line. (1) plane-2d.std: `STAAD PLANE` with 3 joints as `id x y` (Z omitted) + a MEMBER INCIDENCES row + SUPPORTS PINNED; (2) feet-imperial.std: `UNIT FEET KIPS` (imperial default-switch), JOINT COORDINATES in feet, then `UNIT INCH` before MEMBER PROPERTY with a PRIS YD/ZD in inches (mixed-unit per PITFALLS P1); (3) legacy-table.std: `MEMBER PROPERTY STEEL` with unquoted `1 2 TABLE ST W12X35` legacy syntax (PITFALLS P3); (4) continuations.std: a `" -"` continuation line inside MEMBER INCIDENCES, a `*` comment line, a semicolon-packed JOINT COORDINATES line, and an abbreviated command (`MEMB INCI` for MEMBER INCIDENCES per PITFALLS P2 abbreviations); (5) unknown-commands.std: a valid small deck interleaved with an unknown command block (`FROBNICATE 12 34` with body lines) that must be skipped with a warning, not fatal. Keep expected counts consistent with what manifest.ts will assert (Task 1 constants must match these files).</action>
  <verify>
    <automated>cd packages/parser; node -e "const fs=require('fs');const names=['plane-2d','feet-imperial','legacy-table','continuations','unknown-commands'];for(const n of names){const p='test/fixtures/handwritten/'+n+'.std';if(!fs.existsSync(p)){console.error('missing',p);process.exit(1)}const t=fs.readFileSync(p,'utf8');if(!t.includes('* end of fixture')){console.error('missing terminator',p);process.exit(1)}console.log(n,t.split('\n').length,'lines')}"</automated>
  </verify>
  <done>All 5 hand-written fixtures exist, are < 40 lines, end with the terminator comment, and match the counts recorded in manifest.ts expectedHandwritten.</done>
</task>

<task type="auto">
  <name>Task 3: Hermetic loader + smoke test</name>
  <files>packages/parser/test/fixtures/loadFixture.ts, packages/parser/test/fixtures/fixtures.test.ts</files>
  <action>Implement loadFixture.ts: export FIXTURE_DIR resolved relative to the module file (import.meta.url-based path resolution — never process.cwd()-dependent), export FIXTURE_NAMES = ['real/HPP_Main_Building_2.std', 'handwritten/plane-2d.std', ...], and `loadFixture(name: string): string` returning UTF-8 text, throwing on unknown names. fixtures.test.ts smoke test: for every FIXTURE_NAMES entry assert loadFixture returns non-empty text, real fixture length > 50_000 chars, every hand-written fixture contains the `* end of fixture` terminator, and manifest.ts expectedReal counts are all positive integers and expectedHandwritten entries exist for all 5 hand-written names.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/fixtures/fixtures.test.ts</automated>
  </verify>
  <done>fixtures.test.ts green: all fixtures load, counts manifest is complete and positive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture text → future parser | Fixtures are the corpus that exercises untrusted-input handling; they are test data, not production input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | real fixture provenance | mitigate | Byte-identical copy verified (byte count asserted); fixture is the user-provided ground-truth sample, not modified |
| T-03-02 | DoS | manifest counting helpers | accept | Counting helpers run only over known small fixtures at test time; no unbounded input |
| T-03-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- Byte-count equality between source and copied real fixture
- `npx vitest run test/fixtures/fixtures.test.ts` green
- manifest.ts has expectedReal + expectedHandwritten with positive integers
</verification>

<success_criteria>
- Hermetic corpus inside packages/parser/test/fixtures (real + 5 hand-written)
- Deterministic loadFixture + manifest; smoke tests green
- Corpus covers D-01 real file + D-02 edge cases (2D, imperial, legacy TABLE, continuations/abbreviations, unknown commands)
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-03-SUMMARY.md` when done
</output>