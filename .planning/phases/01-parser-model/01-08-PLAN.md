---
phase: 01-parser-model
plan: 8
type: execute
wave: 5
depends_on: [01-04, 01-05]
files_modified:
  - packages/parser/src/types.ts
  - packages/parser/src/staad/loads.ts
  - packages/parser/src/staad/skipped.ts
  - packages/parser/test/staad/loads.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-03]
must_haves:
  truths:
    - "LOAD n LOADTYPE ... TITLE ... blocks produce typed LoadCase records with SELFWEIGHT / MEMBER LOAD / JOINT LOAD items"
    - "LOAD COMB blocks produce combination LoadCase records with factor terms"
    - "ELEMENT LOAD rows inside load blocks produce SKIPPED_ELEMENT warnings (plates out of scope, D-07)"
    - "Non-model commands (PERFORM ANALYSIS, CHANGE, PRINT, PAGE, DEFINE WIND LOAD internals) are tolerated with IGNORED_COMMAND warnings or silently, never fatal"
  artifacts:
    - path: packages/parser/src/staad/loads.ts
      provides: "LOAD / LOAD COMB / SELFWEIGHT / MEMBER LOAD / JOINT LOAD handlers"
      exports: ["loadHandler", "loadCombHandler", "selfweightHandler", "memberLoadHandler", "jointLoadHandler"]
    - path: packages/parser/src/staad/skipped.ts
      provides: "Tolerated-command registrations (ELEMENT LOAD, PERFORM ANALYSIS, etc.)"
      exports: ["registerSkippedCommands"]
  key_links:
    - from: "packages/parser/src/staad/loads.ts"
      to: "packages/parser/src/staad/lists.ts"
      via: "expands LIST member ranges in SELFWEIGHT/MEMBER LOAD rows"
      pattern: "expandList"
    - from: "packages/parser/src/staad/loads.ts"
      to: "packages/parser/src/resolve-units.ts"
      via: "records force units on LoadItem magnitudes for display (P1: force units matter for load display)"
      pattern: "ctx.units"
---

<objective>
Implement load-case parsing (LOAD / LOAD COMB / SELFWEIGHT / MEMBER LOAD / JOINT LOAD) and the tolerated-command set.

Purpose: PARSE-01's load half + PARSE-03 tolerance for the analysis/design commands a viewer does not need. Load arrows and the load-case selector (Phase 3 MARK) consume these LoadCase records; force units are recorded for display per P1.

Output: loads.ts, skipped.ts + tests.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@packages/parser/src/types.ts
@packages/parser/src/core.ts
@staadSample/HPP_Main_Building_2.std
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LOAD + LOAD COMB case headers (TDD)</name>
  <files>packages/parser/src/staad/loads.ts, packages/parser/test/staad/loads.test.ts</files>
  <behavior>
    - Test 1: header `LOAD 1 LOADTYPE Dead TITLE D` → LoadCase { id:1, title:'D', loadtype:'Dead' }
    - Test 2: header `LOAD 13 LOADTYPE Seismic-H TITLE EX` → loadtype 'Seismic-H'
    - Test 3: `LOAD COMB 100 COMB - 1 DL + 1 H` → LoadCase type COMBINATION with terms [{caseId:1, factor:1}, {caseId:1, factor:1}] referencing case name 'DL','H' — store factors keyed by referenced case title when numeric ids absent; tolerant
    - Test 4: bare `LOAD 2` (no LOADTYPE/TITLE) → LoadCase with empty title, no crash
  </behavior>
  <action>Write loads.test.ts first (RED) with these header cases, then implement loadHandler + loadCombHandler (GREEN). loadHandler parses the case header from the BLOCK HEADER ENTRY tokens: LOAD <id> [LOADTYPE <t>] [TITLE <rest...>] — in real STAAD.Pro files the `LOAD 1 LOADTYPE Dead TITLE D` line IS the block header entry and the block body is empty (checker #9; e.g. HPP line 1024 `LOAD 3 LOADTYPE Roof Live TITLE LR`); creates ctx.currentLoadCase and appends to ctx.loadCases. Tolerances: a non-numeric id (e.g. `LOAD R1 LOADTYPE Mass`, absorbed inside DEFINE REFERENCE LOADS but defensively) → MALFORMED_LINE warning + case skipped; a `LOAD LIST ...` block (e.g. `LOAD LIST ENV 1 2` at real line 1688) is a load-case-envelope directive → skipped silently, no case created; missing LOADTYPE/TITLE tolerated. loadCombHandler parses `LOAD COMB <id> COMB - <f1> <name1> + <f2> <name2> ...` into combination terms (factor + referenced case id/title); combination cases are LoadCase records with items [] and a terms field (extend LoadItem with a kind 'COMBINATION_TERM' carrying factor + ref, or store terms on LoadCase — choose: add optional terms to LoadCase in types.ts via a minimal edit in this plan's task, preserving D-03 backward fields). Register both under COMMAND_TABLE with canonical keys 'LOAD' and 'LOAD COMB'.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/loads.test.ts</automated>
  </verify>
  <done>Load-case headers green: id/title/loadtype parsed; combinations parsed tolerantly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SELFWEIGHT / MEMBER LOAD / JOINT LOAD items (TDD)</name>
  <files>packages/parser/src/staad/loads.ts, packages/parser/test/staad/loads.test.ts</files>
  <behavior>
    - Test 1: `SELFWEIGHT Y -1 LIST 1 TO 12 15 TO 18` → LoadItem kind SELFWEIGHT, axis Y, magnitude -1, targets [1..12,15..18]
    - Test 2: `17 18 20 TO 48 UNI GY -27.6` → MEMBER_LOAD, axis GY (global Y), magnitude -27.6, targets expanded
    - Test 3: `303 FY -123` → JOINT_LOAD, axis FY, magnitude -123, targets [303]
    - Test 4: `ELEMENT LOAD` + `132 TO 162 PR GY -28` → SKIPPED_ELEMENT warning, no item added (plates out of scope, D-07)
    - Test 5: malformed `1 TO 2` alone (no direction/magnitude) → MALFORMED_LINE warning, skipped
  </behavior>
  <action>Extend loads.test.ts with these cases (RED), then implement selfweightHandler, memberLoadHandler, jointLoadHandler, and elementLoad skipping (GREEN). SELFWEIGHT row: `SELFWEIGHT <axis> <factor> LIST <list>` → LoadItem { kind:'SELFWEIGHT', axis, magnitude:factor, targets }. MEMBER LOAD row: `<list> <type> <dir> <mag>` where type UNI/CON/etc., dir prefixed G (global) else local per PITFALLS UX: GY/GX/GZ = global, Y/X/Z = local; record axisRef 'GLOBAL'|'LOCAL' (Phase 3 MARK-04 arrow convention). JOINT LOAD row: `<joints> <dir> <mag>`. ELEMENT LOAD rows → SKIPPED_ELEMENT warning with line (D-07), no items. Malformed → MALFORMED_LINE, continue. Store ctx.units.force on LoadCase (declared force unit for display per P1). Register all under COMMAND_TABLE.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/loads.test.ts</automated>
  </verify>
  <done>Load-item handlers green incl. global/local axis convention, element-load skip, malformed warn.</done>
</task>

<task type="auto">
  <name>Task 3: Tolerated commands + load mini-deck integration</name>
  <files>packages/parser/src/staad/skipped.ts, packages/parser/test/staad/loads.test.ts</files>
  <action>Implement skipped.ts: registerSkippedCommands() registers tolerant handlers for commands the viewer ignores, each a no-op-or-warn handler (IGNORED_COMMAND warning with line for analysis/design commands; silent skip acceptable for structural terminators):
- SKIPPED_ELEMENT (D-07): ELEMENT INCIDENCES (real line 375 `ELEMENT INCIDENCES SHELL` — checker #6: without this registration the golden Test 8 SKIPPED_ELEMENT assertion can never fire), ELEMENT LOAD, ELEMENT PROPERTY.
- IGNORED_COMMAND or silent skip: MEMBER RELEASE, MEMBER CRACKED, MEMBER OFFSET, DEFINE MATERIAL, DEFINE REFERENCE LOADS, DEFINE IBC, DEFINE WIND LOAD, DEFINE ENVELOPE, END DEFINE ENVELOPE, START JOB INFORMATION, END JOB INFORMATION, START CONCRETE DESIGN, END CONCRETE DESIGN, SET NL, PARAMETER, CHECK CODE, DESIGN BEAM, DESIGN COLUMN, PERFORM ANALYSIS, CHANGE, PRINT, PAGE, FINISH, LOAD LIST, INPUT WIDTH (checker #7 — every key verified present in HPP_Main_Building_2.std).
These registrations make real STAAD.Pro files parse without UNKNOWN_COMMAND noise for known-but-unneeded commands (D-07 unknown-command warning stays meaningful for truly unknown commands). Then extend the mini-deck integration test: import ALL handler modules incl. units, header, loads, skipped, run parseStaad over a deck opening with `STAAD SPACE` + `UNIT METER KN` and containing 2 LOAD cases (SELFWEIGHT + MEMBER LOAD UNI GY + JOINT LOAD FY + one ELEMENT LOAD row) and 1 LOAD COMB + a PERFORM ANALYSIS line; assert model.units.length === 'M', exact loadCase counts, item kinds/axes/magnitudes, SKIPPED_ELEMENT warning for the element row, IGNORED_COMMAND warning for PERFORM ANALYSIS, and no throw.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/loads.test.ts</automated>
  </verify>
  <done>skipped.ts registered; load mini-deck end-to-end green through dispatch.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOAD block body → LoadCase/LoadItem records | Untrusted load magnitudes, directions, lists; parsed for display only — never executed. |
| analysis/design commands → tolerance | Known-but-unneeded commands must never break the model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | DoS | load list range expansion | mitigate | expandList bounded (shared); no unbounded allocation |
| T-08-02 | Integrity | load magnitude parsing | mitigate | Strict Number() with NaN/Infinity rejection; malformed rows warn MALFORMED_LINE and are skipped |
| T-08-03 | Tampering | load items never executed | mitigate | LoadItem is pure data (display only); no code path interprets magnitudes as behavior (viewer scope, PROJECT.md) |
| T-08-04 | Integrity | global vs local axis convention | mitigate | axisRef recorded explicitly from G-prefix (PITFALLS UX: GY=global); Phase 3 labels this convention |
| T-08-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- `npx vitest run test/staad/loads.test.ts` green
- Load mini-deck: exact loadCase/loadItem counts, axis conventions, warning codes
- PERFORM ANALYSIS tolerated with IGNORED_COMMAND, never fatal
</verification>

<success_criteria>
- LOAD / LOAD COMB / SELFWEIGHT / MEMBER LOAD / JOINT LOAD parsed into typed records (PARSE-01)
- ELEMENT LOAD + analysis commands tolerated per PARSE-03/D-07
- Load items carry global/local axis convention for Phase 3 markers
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-08-SUMMARY.md` when done
</output>