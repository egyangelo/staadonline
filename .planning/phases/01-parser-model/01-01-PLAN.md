---
phase: 01-parser-model
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parser/package.json
  - packages/parser/tsconfig.json
  - packages/parser/vitest.config.ts
  - packages/parser/src/types.ts
  - packages/parser/test/types-smoke.test.ts
autonomous: false
requirements: [PARSE-05]
must_haves:
  truths:
    - "A standalone headless parser package exists with TypeScript strict config and Vitest wired"
    - "Typed records for Node, Member, SectionProfile, SectionMeta, Support, LoadCase, LoadItem, Group, Bounds, ParseWarning, StaadModel, ParseResult compile"
    - "Parse warnings carry stable machine-readable codes and a severity"
  artifacts:
    - path: packages/parser/package.json
      provides: "Parser package manifest (@staad-online/parser), test script"
      contains: "vitest"
    - path: packages/parser/src/types.ts
      provides: "Complete typed model per D-03/D-04/D-06"
      contains: "export interface Node"
    - path: packages/parser/src/types.ts
      provides: "Stable warning code constants"
      contains: "WARNING_CODES"
  key_links:
    - from: "packages/parser/src/types.ts"
      to: "packages/parser/src/index.ts"
      via: "ParseResult is the return type of parseStaad (created in 01-04)"
      pattern: "ParseResult"
---

<objective>
Scaffold the standalone headless parser package and define the complete typed model.

Purpose: Everything downstream (tokenizer, handlers, finalize, Phase 2 renderer) consumes the types defined here. Establishing the package boundary now keeps the parser hermetic and worker-ready (zero DOM/Three imports) per ARCHITECTURE.md Pattern 1.

Output: `packages/parser/` package with package.json, tsconfig, vitest config, and `src/types.ts` defining the full typed model.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for npm install</name>
  <what-built>Nothing yet — pre-install legitimacy gate. The npm packages to be installed are typescript and vitest (build/test tooling only; the parser itself has zero runtime dependencies per STACK.md).</what-built>
  <action>Do NOT run npm install until this gate is approved. The packages slated for install are typescript and vitest, rated [ASSUMED] mainstream build tooling per STACK.md; the Package Legitimacy Gate protocol requires a blocking-human verification before any package-manager install. Present the two npm URLs to the human and wait for the resume signal. If either package looks wrong (typosquat, low downloads, wrong publisher), stop and report instead of installing.</action>
  <files>packages/parser/package.json (devDependencies — install happens in Task 2 after approval)</files>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/typescript and https://www.npmjs.com/package/vitest in a browser.
    2. Confirm both are the well-known mainstream packages (TypeScript by Microsoft; Vitest by the Vite team) with high download counts — not typosquats or lookalikes.
    3. Reply "approved" to resume, or describe concerns.
  </how-to-verify>
  <verify>
    <human-check>Human confirms both npmjs.com pages show the legitimate mainstream packages; no typosquat lookalikes.</human-check>
  </verify>
  <done>Human approved the two packages on npmjs.com (or raised concerns that stopped the install).</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold parser package + install tooling</name>
  <files>packages/parser/package.json, packages/parser/tsconfig.json, packages/parser/vitest.config.ts</files>
  <action>Create the standalone package at packages/parser (repo root is the workspace; Phase 2's Vite app will import this package by name). package.json: name "@staad-online/parser", private true, type "module", main/exports "./src/index.ts" (source-first; bundler resolves TS), scripts.test "vitest run", devDependencies typescript + vitest (latest stable). tsconfig.json: strict true, target ES2022, module ESNext, moduleResolution "bundler", lib ES2022, noEmit true (vitest/esbuild transpiles), include src and test. vitest.config.ts: default config with test.include "test/**/*.test.ts". Then run `npm install` inside packages/parser. Do NOT add any runtime dependencies — the parser is dependency-free by design (ARCHITECTURE.md). Do NOT scaffold React/Vite/Three — Phase 1 is headless only.</action>
  <verify>
    <automated>cd packages/parser; npx vitest --version</automated>
  </verify>
  <done>package.json/tsconfig/vitest.config.ts exist; `npx vitest --version` prints a version; no runtime dependencies in package.json.</done>
</task>

<task type="auto">
  <name>Task 3: Define complete typed model + warning codes</name>
  <files>packages/parser/src/types.ts, packages/parser/test/types-smoke.test.ts</files>
  <action>Implement src/types.ts per D-03 with every field downstream consumers (renderer, inspector, summary panel) need, and per D-04 keeping original 1-based .std numbers as source-of-truth id fields. Define and export: UnitLength ('MM'|'CM'|'DM'|'M'|'FT'|'IN'|'KM'), UnitForce ('N'|'DN'|'KN'|'NE'|'MN'|'MT'|'KG'|'LB'|'KIP'|'PO'), UnitSystem { length, force }; Node { id (1-based source), x, y, z (meters) }; Member { id (1-based source), startNode, endNode (source node ids), sectionKey?, group?, material? }; SectionProfile { label, points: [number,number][] (section outline polygon), approximate: boolean }; SectionMeta { family: 'STEEL'|'CONCRETE'|'PRISMATIC'|'UNKNOWN', dims?: string, area?: number, Ix?: number, Iy?: number }; Support { nodeId (source), type: 'PINNED'|'FIXED'|'FIXED_BUT'|'ENFORCED'|'SPRING'|'PLATE', releases: { FX,FY,FZ,MX,MY,MZ: boolean }, line }; LoadItem { kind: 'MEMBER_LOAD'|'JOINT_LOAD'|'SELFWEIGHT'|'ELEMENT_LOAD'|'GENERATED', axis: 'X'|'Y'|'Z', axisRef: 'GLOBAL'|'LOCAL', magnitude, targets: number[], line }; LoadCase { id (source), title, loadtype?, items: LoadItem[] }; Group { name, memberIds: number[], jointIds: number[], elementIds: number[] }; Bounds { min: [number,number,number], max: [number,number,number] }; ParseWarning { code, message, line, severity: 'error'|'warning'|'info' }; StaadModel { nodes, members, sections: Map<string, SectionProfile & SectionMeta>, supports, loadCases, groups: Map<string, Group>, units: UnitSystem, bounds: Bounds }; ParseResult { model: StaadModel, warnings: ParseWarning[] }. Per D-06 define WARNING_CODES const with stable codes: UNKNOWN_COMMAND, IGNORED_COMMAND, SKIPPED_ELEMENT, UNRESOLVED_SECTION, MALFORMED_LINE, UNIT_CHANGE. Zero DOM/Three imports anywhere in this file (headless + worker-ready constraint, ARCHITECTURE.md Pattern 1). Do NOT add any section-database fields or bundling (D-05 defers the full section DB to Phase 2). types-smoke.test.ts: imports types and WARNING_CODES, asserts every code has an entry and that constructing a ParseWarning with each code compiles and carries a severity; also asserts SectionProfile.approximate exists as boolean (contract for D-05 flag).</action>
  <verify>
    <automated>cd packages/parser; npx tsc --noEmit; npx vitest run test/types-smoke.test.ts</automated>
  </verify>
  <done>tsc passes strict; types-smoke test green; types.ts has zero DOM/Three imports and zero runtime imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.std` text → parser types | Types are compile-time contracts; the trust boundary materializes in 01-04/01-05 when untrusted text reaches parsing. This plan only fixes the shape (Map-based containers, no eval-prone structures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (typescript, vitest) | mitigate | slopcheck + blocking-human checkpoint Task 1 verifies on npmjs.com before install; packages [ASSUMED] mainstream build tooling per STACK.md |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking-human checkpoint for [ASSUMED] packages (Task 1) |
| T-01-02 | Information Disclosure | types.ts | accept | Types carry no data; the privacy guarantee (files never leave device) is enforced at the parse/UI layer, not here |
</threat_model>

<verification>
- `npx tsc --noEmit` strict-clean inside packages/parser
- `npx vitest run` green (types-smoke)
- WARNING_CODES exports exactly the six D-07 codes; no extra runtime deps in package.json
</verification>

<success_criteria>
- packages/parser builds and tests green
- Complete typed model exists per D-03/D-04 with 1-based source ids preserved as fields
- Warnings shape `{ code, message, line, severity }` with stable codes per D-06
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-01-SUMMARY.md` when done
</output>