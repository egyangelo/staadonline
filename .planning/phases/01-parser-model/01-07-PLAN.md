---
phase: 01-parser-model
plan: 7
type: execute
wave: 6
depends_on: [01-04, 01-05, 01-06]
files_modified:
  - packages/parser/src/staad/supports.ts
  - packages/parser/src/staad/groups.ts
  - packages/parser/test/staad/supports.test.ts
  - packages/parser/test/staad/groups.test.ts
  - packages/parser/test/staad/supports-groups.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-05]
must_haves:
  truths:
    - "SUPPORTS rows (PINNED / FIXED / FIXED BUT / ENFORCED with ranges) produce typed Support records per joint"
    - "START GROUP DEFINITION blocks (JOINT / ELEMENT / MEMBER sections with _NAME + ranges) produce typed Group records with expanded member ids"
    - "Skipped element groups and plate supports are tolerated with SKIPPED_ELEMENT warnings (D-07)"
  artifacts:
    - path: packages/parser/src/staad/supports.ts
      provides: "SUPPORTS command handler (restraint types, ranges)"
      exports: ["supportsHandler"]
    - path: packages/parser/src/staad/groups.ts
      provides: "START/END GROUP DEFINITION handler"
      exports: ["startGroupsHandler", "endGroupsHandler"]
  key_links:
    - from: "packages/parser/src/staad/groups.ts"
      to: "packages/parser/src/staad/lists.ts"
      via: "expands _NAME ranges into member/joint/element id lists"
      pattern: "expandList"
    - from: "packages/parser/src/staad/supports.ts"
      to: "packages/parser/src/staad/lists.ts"
      via: "expands joint ranges in support rows"
      pattern: "expandList"
---

<objective>
Implement SUPPORTS and GROUP DEFINITION handlers.

Purpose: PARSE-01's support half + the Group records D-03 needs (renderer highlights, summary panel group list, and Phase 2 color-by-group). Group definitions also drive named-section resolution at finalize (from 01-06). Plate/plane supports and element groups are skipped with warnings per D-07.

Output: supports.ts, groups.ts + tests.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@packages/parser/src/types.ts
@packages/parser/src/core.ts
@staadSample/HPP_Main_Building_2.std
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SUPPORTS handler (TDD)</name>
  <files>packages/parser/src/staad/supports.ts, packages/parser/test/staad/supports.test.ts</files>
  <behavior>
    - Test 1: row '14 16 18 20 22 24 1118 1121 PINNED' → 8 Support records, type PINNED
    - Test 2: row '1 TO 5 FIXED' → 5 FIXED supports
    - Test 3: '7 FIXED BUT FY MZ' → type FIXED_BUT with releases { FY:false, MZ:false } (released dof set)
    - Test 4: plate support row '_RAFT PLATE MAT DIRECT Y SUBGRADE 15000 COMPRESSION' → no Support record, SKIPPED_ELEMENT warning, no crash
    - Test 5: malformed row 'X PINNED' → MALFORMED_LINE warning, skipped
  </behavior>
  <action>Write supports.test.ts first (RED), then implement (GREEN). Handler iterates body rows: leading tokens are a joint list (expandList) — note STAAD SUPPORTS references JOINT numbers (nodes); trailing tokens define restraint: PINNED (translations fixed), FIXED (all 6 fixed), FIXED BUT <dofs> (all fixed except listed releases), ENFORCED, SPRING with stiffness (record type + note), else tolerant warn MALFORMED_LINE. Rows referencing a group name (_NAME ... PLATE ...) are plate supports — skipped with SKIPPED_ELEMENT warning (D-07; plates out of scope per phase boundary). Create Support { nodeId, type, releases: {FX,FY,FZ,MX,MY,MZ}, line } per D-03. If a joint id has no Node record, still store the support (tolerant; finalize may warn) — or warn DANGLING_REF with MALFORMED_LINE code; choose: store + MALFORMED_LINE warning. Tests use createContext with seeded nodes.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/supports.test.ts</automated>
  </verify>
  <done>Supports green: PINNED/FIXED/FIXED BUT/releases, ranges, plate-skip warning, malformed warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GROUP DEFINITION handler (TDD)</name>
  <files>packages/parser/src/staad/groups.ts, packages/parser/test/staad/groups.test.ts</files>
  <behavior>
    - Test 1: START GROUP DEFINITION / MEMBER section `_COLS 1 TO 12 49 TO 57` → Group { name:'_COLS', memberIds:[1..12,49..57] }
    - Test 2: JOINT section `_F7 303` → Group with jointIds [303]
    - Test 3: ELEMENT section `_RAFT 132 TO 162 211` → Group with elementIds [132..162,211] AND SKIPPED_ELEMENT warning (element groups skipped, D-07)
    - Test 4: continuation lines within a group row (line ending ' -') merge into the same group
    - Test 5: unknown section keyword inside group block → tolerated, no crash
  </behavior>
  <action>Write groups.test.ts first (RED), then implement (GREEN). startGroupsHandler consumes block body until END GROUP DEFINITION is reached (blocks.ts already scoped this block per 01-04); parse subsection headers JOINT / ELEMENT / MEMBER; rows are `_NAME <list>` with list expanded via expandList; continuation lines already joined by the tokenizer. Store Group { name, memberIds, jointIds, elementIds } into ctx.groups Map. ELEMENT-section groups produce SKIPPED_ELEMENT warnings (plates/solids out of scope, D-07). Unknown subsection keywords → tolerant ignore. Test END handling: endGroupsHandler exists as a registered no-op for the END header.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/groups.test.ts</automated>
  </verify>
  <done>Groups green: MEMBER/JOINT/ELEMENT sections, ranges, continuations, skip warning for elements.</done>
</task>

<task type="auto">
  <name>Task 3: Wire supports+groups into dispatch (mini-deck integration)</name>
  <files>packages/parser/test/staad/supports-groups.test.ts</files>
  <action>Build an integration-style test: construct a mini STAAD deck opening with `STAAD SPACE` and `UNIT METER KN` (exercising the 01-04 core-state handlers end-to-end — checker #3 STAAD coverage), then JOINT COORDINATES (6 joints), MEMBER INCIDENCES, START GROUP DEFINITION (MEMBER + ELEMENT sections with a continuation), SUPPORTS (PINNED range + a plate row), MEMBER PROPERTY PRIS rows, and CONSTANTS MATERIAL. Import ALL handler modules for side-effect registration into COMMAND_TABLE (units, header, joint-coordinates, member-incidences, member-property, steel-resolver, supports, groups, constants), run parseStaad over the deck text, and assert: no throw; model.units.length === 'M' (UNIT applied, not FEET default); node/member/support/group counts exact; support types correct; group memberIds exact; SKIPPED_ELEMENT warning present for the element group and plate support; UNRESOLVED_SECTION absent (all PRIS); member materials set. This is the first end-to-end dispatch proof before the golden-file plan.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/supports-groups.test.ts</automated>
  </verify>
  <done>End-to-end mini-deck test green through the full dispatch table.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUPPORTS body → Support records | Untrusted joint lists + restraint tokens; ranges must be bounded. |
| GROUP DEFINITION body → Group records | Untrusted name lists + ranges; names become Map keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | DoS | support/group range expansion | mitigate | expandList bounded (shared); no unbounded allocation |
| T-07-02 | Tampering | group name as Map key | mitigate | ctx.groups is Map<string, Group>; name keys sanitized (no prototype keys) per PITFALLS security |
| T-07-03 | Integrity | dangling joint refs in supports | mitigate | Store support + MALFORMED_LINE warning; never throw (tolerant per PARSE-03) |
| T-07-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- `npx vitest run test/staad/supports.test.ts test/staad/groups.test.ts test/staad/supports-groups.test.ts` green
- Mini-deck integration proves dispatch + counts + warnings end-to-end
</verification>

<success_criteria>
- Supports parsed with restraint types + releases per joint (PARSE-01)
- Groups parsed per D-03 with expanded ids; element groups skipped with warnings (D-07)
- First full-dispatch mini-deck test green
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-07-SUMMARY.md` when done
</output>