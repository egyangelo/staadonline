---
phase: 01-parser-model
plan: 9
type: execute
wave: 6
depends_on: [01-05, 01-06, 01-07, 01-08]
files_modified:
  - packages/parser/src/index.ts
  - packages/parser/test/golden.test.ts
  - packages/parser/test/handwritten.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-05]
must_haves:
  truths:
    - "The real HPP_Main_Building_2.std parses without crashing with expected counts (D-01, D-09 tolerant tier)"
    - "Hand-written fixtures parse with exact counts + specific values incl. unit state and member geometry (D-09 exact tier)"
    - "Tolerant fixtures assert warning codes (D-08)"
    - "parseStaad completes the full registerParsingCommands() import wiring (production entry)"
  artifacts:
    - path: packages/parser/src/index.ts
      provides: "registerParsingCommands() now imports ALL staad handler modules (final wiring)"
      contains: "registerParsingCommands"
    - path: packages/parser/test/golden.test.ts
      provides: "Golden-file tests against the real HPP fixture + manifest counts"
      contains: "HPP_Main_Building_2"
    - path: packages/parser/test/handwritten.test.ts
      provides: "Exact-count tests over the five hand-written fixtures"
      contains: "plane-2d"
  key_links:
    - from: "packages/parser/src/index.ts"
      to: "packages/parser/src/staad/skipped.ts"
      via: "registerParsingCommands imports skipped module (side-effect registration)"
      pattern: "registerSkippedCommands"
    - from: "packages/parser/test/golden.test.ts"
      to: "packages/parser/test/fixtures/manifest.ts"
      via: "expected counts imported from manifest"
      pattern: "expectedReal"
---

<objective>
Final wiring + golden-file verification: the full parser against the real STAAD.Pro file and every hand-written fixture.

Purpose: Close the phase by proving the four success criteria — golden corpus parses with correct counts, unit state is correct on mixed-unit files, tolerance never fatal, and the typed model is complete (PARSE-01..05). This is the D-09 dual-tier validation gate.

Output: final `registerParsingCommands()` wiring in index.ts + golden.test.ts + handwritten.test.ts.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@packages/parser/src/index.ts
@packages/parser/test/fixtures/manifest.ts
@packages/parser/test/fixtures/loadFixture.ts
@staadSample/HPP_Main_Building_2.std
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete production wiring</name>
  <files>packages/parser/src/index.ts</files>
  <action>Edit index.ts so registerParsingCommands() imports every handler module for side-effect registration into COMMAND_TABLE: units (UNIT → applyUnitCommand — checker #2 wiring), header (STAAD → ctx.structure — checker #3 wiring), joint-coordinates, member-incidences, member-property, steel-resolver (registration helper), supports, groups, constants, loads, skipped (via registerSkippedCommands). Order: import modules so their module-scope registerCommand calls execute; call registerSkippedCommands() explicitly. Keep parseStaad's signature unchanged (text) → ParseResult; size guard, tokenize, segment, dispatch, finalize as built in 01-04. Do NOT add any section-database code (D-05 defers to Phase 2). Verify by running the full test suite — all prior unit tests plus the new golden tests must pass through the production entry.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run</automated>
  </verify>
  <done>registerParsingCommands imports all handlers; full suite green through parseStaad.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden-file test on the real fixture (TDD)</name>
  <files>packages/parser/test/golden.test.ts</files>
  <behavior>
    - Test 1: parseStaad(loadFixture('real/HPP_Main_Building_2.std')) does not throw and returns ParseResult
    - Test 2: model.nodes.length equals manifest.expectedReal.joints (tolerant tier: assert >= 1200 AND == manifest value)
    - Test 3: model.members.length equals manifest.expectedReal.members
    - Test 4: model.sections.size > 0 (PRIS + TABLE fallback profiles present)
    - Test 5: model.supports.length equals manifest.expectedReal.supports (8 PINNED in the sample)
    - Test 6: model.loadCases.length equals manifest.expectedReal.loadCases = loadPrimary (14) + loadComb (274) = 288 total (checker #9: manifest counts both primary LOAD headers and LOAD COMB lines; assertion is disambiguated — not 14, not 274, but the sum)
    - Test 7: model.groups.size equals manifest.expectedReal.groups (START GROUP DEFINITION names)
    - Test 8: warnings include SKIPPED_ELEMENT (ELEMENT INCIDENCES SHELL at line 375 — now realizable via the skipped.ts registration) and UNRESOLVED_SECTION (TABLE sections); no throw; bounds computed (min/max finite)
    - Test 9 (golden-tier unit assertion, checker #2): model.units.length === 'M' and model.units.force === 'KN' (UNIT METER KN at line 14 applied — a missing UNIT handler would leave the FEET/KIPS default), and node 2 y === -2.8 exactly (line 16 `2 0 -2.8 0`; with the FEET default the value would be -2.8 × 0.3048 ≈ -0.8534, so this assertion catches a 3.28× scale error that count-only tests cannot see)
  </behavior>
  <action>Write golden.test.ts with the above expectations (RED against current manifest), running through the fully wired parseStaad. If manifest values disagree with actual parser output, reconcile by re-checking the fixture's raw content (the fixture is ground truth; the parser must match it) — fix parser bugs, not the manifest. The manifest is deterministic (counting helpers from 01-03 expose expectedReal.loadPrimary (14), expectedReal.loadComb (274), expectedReal.loadCases (288), expectedReal.joints, expectedReal.members, expectedReal.supports, expectedReal.groups); assert == manifest values exactly where counts are exact, and == for supports/loadcases/groups which are exact in the file. Commit as `test(01-09): golden tests on real HPP fixture`.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/golden.test.ts</automated>
  </verify>
  <done>Golden tests green: real file parses with manifest-exact counts, correct warnings, computed bounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hand-written exact-tier tests (TDD)</name>
  <files>packages/parser/test/handwritten.test.ts</files>
  <behavior>
    - Test 1: plane-2d.std → exact joint count, node z values all 0 (structure PLANE set by the STAAD header handler from the `STAAD PLANE` line; Z omitted per P2 — checker #3 coverage), members count exact
    - Test 2: feet-imperial.std → UNIT FEET KIPS default honored; coords converted to meters (1 ft → 0.3048 m); UNIT INCH before PRIS → section yd/zd converted from inches (P1 mixed-unit: coords in feet, properties in inches)
    - Test 3: legacy-table.std → TABLE ST W12X35 → members linked to approximate fallback section, approximate=true, UNRESOLVED_SECTION warning present
    - Test 4: continuations.std → `" -"` continuation lines merge correctly (member count exact), `*` comment ignored, semicolon-packed joints exact, abbreviated MEMB INCI command recognized
    - Test 5: unknown-commands.std → parse succeeds, warnings contain UNKNOWN_COMMAND and/or IGNORED_COMMAND codes, model still has expected nodes/members (tolerance per PARSE-03)
    - Test 6: warning codes asserted per D-08 for every tolerant case
  </behavior>
  <action>Write handwritten.test.ts with exact-count + specific-value assertions per the behavior contract, using loadFixture + fully wired parseStaad (RED). Implement no new parser code — any failure means a parser bug to fix (unit state, 2D handling, continuation, abbreviations). Values asserted are concrete: plane z=0, feet→meters factors, inch→meters section dims, exact counts from manifest.expectedHandwritten. Commit as `test(01-09): exact-tier handwritten fixture tests`.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/handwritten.test.ts</automated>
  </verify>
  <done>All hand-written exact-tier tests green with specific unit/geometry values and warning codes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real STAAD.Pro file → full parser | The golden corpus is the tolerant-tier proof; hostile inputs must warn, never crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | DoS | full pipeline on 92 KB real file | mitigate | Size guard + bounded expansions + linear tokenizer already in place; golden test proves no crash |
| T-09-02 | Integrity | golden counts drift | mitigate | manifest derived by deterministic counting helpers; tests assert == manifest (fixture is ground truth) |
| T-09-03 | Tampering | production wiring imports | mitigate | registerParsingCommands only imports in-repo handler modules; no dynamic import, no eval (PITFALLS security) |
| T-09-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- `npx vitest run` (full suite) green in packages/parser
- Golden + handwritten tests green; warning codes asserted per D-08
- parseStaad proven headless (no DOM/Three imports anywhere in packages/parser/src)
</verification>

<success_criteria>
- Real HPP file parses with manifest-exact counts, correct units, computed bounds (PARSE-01..05, D-01/D-09)
- All hand-written fixtures exact-tier green incl. unit state + member geometry values (D-02/D-09)
- Tolerance proven: unknown/ignored commands warn, never fatal (PARSE-03, D-06/D-07)
- Full test suite green; parser headless + worker-ready (PARSE-05)
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-09-SUMMARY.md` when done
</output>