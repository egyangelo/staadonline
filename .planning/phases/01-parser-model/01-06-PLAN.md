---
phase: 01-parser-model
plan: 6
type: execute
wave: 5
depends_on: [01-04, 01-05]
files_modified:
  - packages/parser/src/staad/member-property.ts
  - packages/parser/src/staad/steel-resolver.ts
  - packages/parser/src/staad/constants.ts
  - packages/parser/test/staad/member-property.test.ts
  - packages/parser/test/staad/steel-resolver.test.ts
  - packages/parser/test/staad/constants.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-05]
must_haves:
  truths:
    - "MEMBER PROPERTY blocks (AMERICAN / quoted DB / bare, PRIS YD/ZD rows and TABLE ST rows) assign sectionKeys to member ranges"
    - "PRIS YD/ZD rows produce rectangular SectionProfile polygons at parse time (D-05)"
    - "TABLE ST rows (quoted and unquoted) produce an approximate fallback SectionProfile flagged approximate=true (D-05) with UNRESOLVED_SECTION warning (D-07)"
    - "CONSTANTS MATERIAL assignments store material on members"
  artifacts:
    - path: packages/parser/src/staad/member-property.ts
      provides: "MEMBER PROPERTY command handler (PRIS + TABLE rows, named and ranged)"
      exports: ["memberPropertyHandler"]
    - path: packages/parser/src/staad/steel-resolver.ts
      provides: "SectionProfile construction: PRIS → rect polygon; TABLE → fallback + approximate flag"
      exports: ["resolveSectionProfile", "prisProfile", "fallbackProfile"]
    - path: packages/parser/src/staad/constants.ts
      provides: "CONSTANTS handler storing MATERIAL per member range"
      exports: ["constantsHandler"]
  key_links:
    - from: "packages/parser/src/staad/member-property.ts"
      to: "packages/parser/src/staad/steel-resolver.ts"
      via: "PRIS YD/ZD tokens → prisProfile(); TABLE tokens → fallbackProfile()"
      pattern: "prisProfile|fallbackProfile"
    - from: "packages/parser/src/staad/member-property.ts"
      to: "packages/parser/src/staad/lists.ts"
      via: "expands member ranges in property rows"
      pattern: "expandList"
---

<objective>
Implement member property parsing and section-profile resolution: MEMBER PROPERTY (all syntax variants) + steel-resolver + CONSTANTS material.

Purpose: PARSE-01's property half + the D-05 section-geometry decision: PRIS YD/ZD becomes a rectangular polygon at parse time; TABLE ST members get an approximate fallback shape + flag. Handles PITFALLS P3 version drift (AMERICAN vs quoted DB, unquoted vs quoted TABLE).

Output: member-property.ts, steel-resolver.ts, constants.ts + tests.
</objective>

<execution_context>
@C:/Users/Work/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Work/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-model/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@packages/parser/src/types.ts
@packages/parser/src/core.ts
@packages/parser/src/resolve-units.ts
@staadSample/HPP_Main_Building_2.std
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Steel section resolver (TDD)</name>
  <files>packages/parser/src/staad/steel-resolver.ts, packages/parser/test/staad/steel-resolver.test.ts</files>
  <behavior>
    - Test 1: prisProfile({ label:'_C1', yd:0.4, zd:0.5 }) → 4-point rectangle polygon: points [±zd/2, ±yd/2] i.e. [[-0.25,-0.2],[0.25,-0.2],[0.25,0.2],[-0.25,0.2]], approximate=false
    - Test 2: prisProfile with unit FEET → yd/zd converted to meters first
    - Test 3: fallbackProfile('W14X90') → approximate=true, small default box profile (e.g. 0.2×0.2 m) with label preserved
    - Test 4: resolveSectionProfile dispatch: 'PRIS' tokens → prisProfile; 'TABLE' tokens → fallbackProfile
  </behavior>
  <action>Write steel-resolver.test.ts first (RED), then implement (GREEN). Per D-05: PRIS YD/ZD → rectangular polygon at parse time (4-corner rectangle centered on origin in the [x,z] section plane, half-extents zd/2 and yd/2). TABLE ST → approximate fallback shape (fixed default box) + approximate=true. Full section DB is DEFERRED to Phase 2 — do NOT implement any database lookup or bundled table here (D-05 boundary). resolveSectionProfile(kind, tokens, units) dispatches on PRIS vs TABLE kind tokens; dims recorded in SectionMeta.dims as raw string. Points are plain [number,number][] — no DOM/Three imports.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/steel-resolver.test.ts</automated>
  </verify>
  <done>Resolver green: rect polygons for PRIS, approximate flag + fallback for TABLE, unit conversion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MEMBER PROPERTY handler (TDD)</name>
  <files>packages/parser/src/staad/member-property.ts, packages/parser/test/staad/member-property.test.ts</files>
  <behavior>
    - Test 1: row '_C1 PRIS YD 0.4 ZD 0.5' under AMERICAN header → named section '_C1' with rect profile (links via groups at finalize)
    - Test 2: row '17 18 20 TO 48 PRIS YD 0.05 ZD 0.05' → members 17,18,20..48 each linked to the PRIS section (memberSectionLinks)
    - Test 3: row '964 TO 977 TABLE 'IPE' ST 'IPE 300'' under quoted DB header 'MEMBER PROPERTY 'EUROPE (EN 2023).DB3'' → members get fallback profile labeled 'IPE 300', approximate=true, UNRESOLVED_SECTION warning
    - Test 4: legacy unquoted '1 3 TABLE ST W12X35' → same fallback behavior
    - Test 5: PRIS with only YD (no ZD) → circular default: approximate fallback + warning (P3: YD alone means circular)
  </behavior>
  <action>Write member-property.test.ts first (RED), then implement (GREEN). Handler reads the block header tokens for the DB qualifier ('AMERICAN', quoted '...DB3', or bare). For each body row: split member-list tokens (expandList) from property tokens; property forms per P3: PRIS YD v [ZD v] (YD-only → circular fallback + approximate), TABLE [quotedName] ST [quotedSection], PIPE/TUBE/USER/TAPERED tolerated as approximate fallback with UNRESOLVED_SECTION warning (not fatal). Named rows (_NAME ...) register ctx.namedSections keyed by name (linked to members via groups in finalize). Ranged rows write ctx.memberSectionLinks (memberId → sectionKey). Section label = table section name or '_' + property name. Unresolved sections always warn UNRESOLVED_SECTION (D-07) and produce approximate profiles (D-05). Quoted tokens arrive quote-stripped from the tokenizer — use them as-is.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/member-property.test.ts</automated>
  </verify>
  <done>Property handler green: named PRIS, ranged PRIS, quoted + legacy TABLE, YD-only circular fallback.</done>
</task>

<task type="auto">
  <name>Task 3: CONSTANTS handler (material)</name>
  <files>packages/parser/src/staad/constants.ts, packages/parser/test/staad/constants.test.ts</files>
  <action>Implement constants.ts and register constantsHandler under 'CONSTANTS', 'MATERIAL', and 'BETA' (checker #5): in the real file (lines 835-849) the uppercase-first-token segmentation rule splits CONSTANTS body rows into their own blocks — `BETA 90 MEMB ...` at line 836, `MATERIAL CONCRETE MEMB ...` at 837, `MATERIAL STEEL_36_KSI MEMB ...` at 846 — so the handler must be reachable through those keys or materials are never stored. When dispatched as 'CONSTANTS', iterate block.bodyLines; when dispatched as 'MATERIAL'/'BETA', read the row from the block header entry tokens PLUS any digit-starting body lines (continuations like lines 838-845). Parse rows of form `MATERIAL <name> MEMB <list>` → set material on the member records in the expanded list (ctx.members lookup by id, set .material per D-03 downstream need for color-by-material in Phase 3). Rows like `BETA 90 MEMB ...` are tolerated and ignored (no warning — they are valid STAAD, not unknown). Malformed MATERIAL rows (non-numeric list) warn MALFORMED_LINE. Tests: MATERIAL CONCRETE MEMB 1 TO 3 sets materials (via both the CONSTANTS-body path and the MATERIAL-block-header path); BETA row ignored; malformed row warns.</action>
  <verify>
    <automated>cd packages/parser; npx vitest run test/staad/constants.test.ts</automated>
  </verify>
  <done>Constants handler green: MATERIAL assignments stored, BETA tolerated, malformed warns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEMBER PROPERTY body → section profiles | Untrusted section names/dims flow into geometry; version-drifted syntax must not crash. |
| CONSTANTS body → member material | Untrusted material names stored on members (display data only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | DoS | property range expansion | mitigate | expandList bounded (shared with 01-05) |
| T-06-02 | Tampering | section label as Map key | mitigate | sections/namedSections are Map<string, ...>; no prototype-pollutable plain objects (PITFALLS security) |
| T-06-03 | Integrity | unit drift on YD/ZD | mitigate | yd/zd converted via ctx.units toMeters before polygon build (P1) |
| T-06-04 | Integrity | dimension inference from names | mitigate | Section sizes NEVER parsed from name strings (P3); PRIS uses explicit YD/ZD only; TABLE always approximate |
| T-06-SC | Tampering | npm installs | mitigate | No installs in this plan (01-01 owns installs; checkpoint approved there) |
</threat_model>

<verification>
- `npx vitest run test/staad/steel-resolver.test.ts test/staad/member-property.test.ts test/staad/constants.test.ts` green
- Real HPP MEMBER PROPERTY sections (lines 799-834) parse into expected counts via the mini-deck test
- No section-database code exists (grep for 'aisc'|'sections.json' in packages/parser — absent)
</verification>

<success_criteria>
- PRIS → rectangular polygons at parse time (D-05, PARSE-01 properties)
- TABLE (quoted + legacy) → approximate fallback + flag + UNRESOLVED_SECTION warning (D-05/D-07)
- Named sections resolve to member ranges via groups at finalize; materials stored via CONSTANTS
</success_criteria>

<output>
Create `.planning/phases/01-parser-model/01-06-SUMMARY.md` when done
</output>